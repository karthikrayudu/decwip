using System;
using System.Linq;
using System.Threading.Tasks;
using Microsoft.CodeAnalysis;
using Microsoft.CodeAnalysis.MSBuild;
using Microsoft.CodeAnalysis.CSharp.Syntax;

class Program
{
    static async Task Main(string[] args)
    {
        Console.Write("Enter Project/Solution Path: ");
        string projectPath = Console.ReadLine();

        Console.Write("Enter Package/Namespace (e.g., Newtonsoft.Json): ");
        string targetNamespace = Console.ReadLine();

        var workspace = MSBuildWorkspace.Create();

        Console.WriteLine("\n🔄 Loading project...");

        var project = await workspace.OpenProjectAsync(projectPath);
        var compilation = await project.GetCompilationAsync();

        Console.WriteLine("✅ Project Loaded Successfully!\n");

        foreach (var tree in compilation.SyntaxTrees)
        {
            var semanticModel = compilation.GetSemanticModel(tree);
            var root = await tree.GetRootAsync();

            var classes = root.DescendantNodes().OfType<ClassDeclarationSyntax>();

            foreach (var cls in classes)
            {
                bool classUsesPackage = false;

                var methods = cls.DescendantNodes().OfType<MethodDeclarationSyntax>();

                foreach (var method in methods)
                {
                    bool methodUsesPackage = false;

                    var invocations = method.DescendantNodes()
                                            .OfType<InvocationExpressionSyntax>();

                    foreach (var invocation in invocations)
                    {
                        var symbolInfo = semanticModel.GetSymbolInfo(invocation);
                        var symbol = symbolInfo.Symbol as IMethodSymbol;

                        if (symbol != null)
                        {
                            string namespaceName = symbol.ContainingNamespace?.ToString();

                            if (!string.IsNullOrEmpty(namespaceName) &&
                                namespaceName.StartsWith(targetNamespace))
                            {
                                if (!methodUsesPackage)
                                {
                                    Console.WriteLine($"\n📄 File: {tree.FilePath}");
                                    Console.WriteLine($"  🏷 Class: {cls.Identifier.Text}");
                                    Console.WriteLine($"    🔹 Method: {method.Identifier.Text}");
                                }

                                Console.WriteLine($"       ➤ Uses: {symbol.Name} ({namespaceName})");

                                methodUsesPackage = true;
                                classUsesPackage = true;
                            }
                        }
                    }
                }

                // Optional: Print class if only class-level usage (rare case)
                if (classUsesPackage)
                {
                    // already printed via method
                }
            }
        }

        Console.WriteLine("\n🎯 Analysis Completed!");
    }
}
