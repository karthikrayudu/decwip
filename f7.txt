 var genericTypes = method.DescendantNodes()
                             .OfType<GenericNameSyntax>();

    foreach (var gen in genericTypes)
    {
        foreach (var arg in gen.TypeArgumentList.Arguments)
        {
            var typeName = arg.ToString();

            if (!result.ClassNames.Contains(typeName))
                result.ClassNames.Add(typeName);
        }
    }
