if (!File.Exists(path))
            throw new FileNotFoundException($"Config file not found: {path}");

        var json = File.ReadAllText(path);

        var data = JsonSerializer.Deserialize<EventData>(json);

        if (data == null)
            throw new Exception("Failed to load EventData from JSON");

        return data;
